node {
    checkout scm

    docker.withRegistry('https://hub.docker.com/repository/docker/jolloway95/tshapeproject/general', 'dockerhub credentials') {

        def customImage = docker.build("my-image:${env.BUILD_ID}")

        /* Push the container to the custom Registry */
        customImage.push()
    }
}